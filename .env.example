# Copy this file to .env.local and fill in real values.
# Never commit .env.local — it is already in .gitignore.

# Web3Forms access key (public by design, but keep centralized here instead of hardcoded).
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=cae3309e-c4f5-4e30-9d63-7a085e6898a8

# Business contact info, centralized so it is never duplicated across pages.
NEXT_PUBLIC_BUSINESS_EMAIL=info@hgamultiservi.com
NEXT_PUBLIC_BUSINESS_PHONE=+13054988610

# --- Stripe (NOT wired up yet — placeholder for the next phase) ---------
# Server-only secret keys. NEVER prefix these with NEXT_PUBLIC_.
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# Publishable key is safe for the client if/when Stripe Elements is used.
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# --- Operational integration (Make.com / Google Sheets) — not wired up yet
# MAKE_WEBHOOK_URL=
